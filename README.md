# Raspberry GPIO Shutdown

Shuts down the Raspberry Pi when the input on pin 11 is high.

## Installation

```bash
$ npm install --global git+ssh://git@github.com:nikovanmeurs/raspberry-gpio-shutdown.git
```

## Usage

```bash
$ shutdown-pi
```